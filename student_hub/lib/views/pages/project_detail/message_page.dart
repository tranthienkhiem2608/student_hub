import 'package:flutter/material.dart';

class MessageProjectPage extends StatelessWidget {
  const MessageProjectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Message Page'),
      ),
    );
  }
}