import 'package:flutter/material.dart';

class HiredPage extends StatelessWidget {
  const HiredPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hired Page'),
      ),
    );
  }
}